let length = parseFloat(prompt("Enter length of rectangle:"));
let breadth = parseFloat(prompt("Enter breadth of rectangle:"));

const rectangleArea = (l, b) => l * b;

console.log("Area of Rectangle:", rectangleArea(6, 4));
